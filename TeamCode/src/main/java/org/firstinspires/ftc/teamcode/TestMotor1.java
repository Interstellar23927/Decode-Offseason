package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.hardware.Gamepad;

public class TestMotor1 {
    HardwareRobot robot;

    public TestMotor1(HardwareRobot r) {robot = r;}
    public void Update(Gamepad gamepad1){
        /*if (gamepad1.dpad_up && !robot.toSensor.isPressed()) {
            robot.testMotor1.setPower(0.5);
        } else {
            robot.testMotor1.setPower(0);
        }*/
    }
}
